{% extends 'base.html.twig' %}

{% block title %}Modifier votre Mot de passe !{% endblock %}

{% block body %}
 <div class="contenaireAcount">
    <div class="formAcount">
    <h1>Modiffier mon mot de passe</h1>
        {% if notification %}

        <div class="alert ">
            {{ notification }}
        </div>
        {% endif %}
        {{ form_start(form) }}
        
            {{ form_errors(form) }}
            {{ form_row(form.speudo) }}
            {{ form_row(form.old_password) }}
            {{ form_row(form.new_password) }}
          
            <div class="bottumChat">
                {{ form_row(form.submit) }}
           
                <div class="retourchat">
                    
                    <a class="submitChangePaswordRetrour" href="{{ path('chat')}}">Retourner sur le chat</a>
                </div>
            </div>

        {{ form_end(form) }} 
        

    </div>
 </div>
{% endblock %}
